# twinx-sandbox-k8s

Minimal CNI-only SmartX member-cluster values for the ScaleX public sandbox.

This repository is sandbox-validated migration scaffolding, not a production-live
cluster definition. It contains no secrets, credentials, or app patches. Replace
the documentation-only API address before rendering it with:

- Base repo: https://github.com/SmartX-Team/smartx-k8s.git
- Base revision: scalex-sandbox-smartx-k8s
- Enabled feature: org.ulagbulag.io/cni
